<reading-questions><title> Check your Understanding </title>

	<exercise><title>The function <m>\frac{s - 4}{s^2 - 16}</m> can be split into separate terms that match the forms for sine and cosine inverse Laplace transforms</title>
		<statement correct="yes"/>
		<feedback>
			<p>
				True. This function can be split as <m>\frac{s}{s^2 - 16} - \frac{4}{s^2 - 16}</m>, allowing you to use the sine and cosine inverse Laplace transforms on each term.
			</p>
		</feedback>
	</exercise>
	
	<exercise><title>Which of the following functions can be split into separate terms that match known inverse Laplace transforms?</title>
		<statement/>
		<choices>
			<choice>
				<statement><m>\frac{3}{s^2 + 4}</m></statement>
				<feedback>
					<p>
						Incorrect. This function already matches a known sine form and doesn't require splitting.
					</p>
				</feedback>
			</choice>
			<choice correct="yes">
				<statement><m>\frac{s + 2}{s^2 + 9}</m></statement>
				<feedback>
					<p>
						Correct! This function can be split as <m>\frac{s}{s^2 + 9} + \frac{2}{s^2 + 9}</m>, matching cosine and sine forms.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement><m>\frac{5}{(s - 3)^2 + 25}</m></statement>
				<feedback>
					<p>
						Incorrect. This function already matches a known sine form and doesn't require splitting.
					</p>
				</feedback>
			</choice>
		</choices>
	</exercise>
	
	<exercise><title>The function <m>\frac{s - 6}{s^2 - 36}</m> can be split into <fillin characters="40"/></title>
		<statement/>
		<choices>
			<choice correct="yes">
				<statement>\frac{s}{s^2 - 36} - \frac{6}{s^2 - 36}</statement>
				<feedback>
					<p>
						Correct! Splitting the function into these two terms allows you to use cosine and sine inverse Laplace transforms on each.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement>\frac{s}{s^2 - 36} + \frac{6}{s^2 - 36}</statement>
				<feedback>
					<p>
						Incorrect. The second term should be subtracted, not added.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement>\frac{s}{s^2 - 36} - \frac{3}{s^2 - 36}</statement>
				<feedback>
					<p>
						Incorrect. The correct value for the second term is <m>6</m>, not <m>3</m>.
					</p>
				</feedback>
			</choice>
		</choices>
	</exercise>
	
	<exercise><title>What is the correct inverse Laplace transform of the function <m>\frac{s + 1}{s^2 - 1}</m> after splitting it into two terms?</title>
		<statement/>
		<choices>
			<choice>
				<statement><m>\cosh(t) + \sinh(t)</m></statement>
				<feedback>
					<p>
						Correct! The function can be split as <m>\frac{s}{s^2 - 1} + \frac{1}{s^2 - 1}</m>, which corresponds to <m>\cosh(t)</m> and <m>\sinh(t)</m>.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement><m>\cosh(2t) + \sinh(2t)</m></statement>
				<feedback>
					<p>
						Incorrect. The correct transforms involve <m>\cosh(t)</m> and <m>\sinh(t)</m>, not <m>2t</m>.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement><m>\cos(t) + \sin(t)</m></statement>
				<feedback>
					<p>
						Incorrect. The function corresponds to hyperbolic sine and cosine, not regular sine and cosine.
					</p>
				</feedback>
			</choice>
		</choices>
	</exercise>
	
	<exercise><title>Splitting the function <m>\frac{s - 3}{s^2 + 9}</m> results in terms that correspond to inverse Laplace transforms of cosine and sine</title>
		<statement correct="yes"/>
		<feedback>
			<p>
				True. Splitting this function into <m>\frac{s}{s^2 + 9} - \frac{3}{s^2 + 9}</m> allows you to apply cosine and sine inverse Laplace transforms.
			</p>
		</feedback>
	</exercise>
	
	<exercise><title>The function <m>\frac{s - 7}{s^2 - 49}</m> can be split into two terms: <m>\frac{s}{s^2 - 49}</m> and <fillin characters="12"/></title>
		<statement/>
		<choices>
			<choice correct="yes">
				<statement>\frac{-7}{s^2 - 49}</statement>
				<feedback>
					<p>
						Correct! The function can be split into <m>\frac{s}{s^2 - 49}</m> and <m>\frac{-7}{s^2 - 49}</m>.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement>\frac{7}{s^2 - 49}</statement>
				<feedback>
					<p>
						Incorrect. The second term should have a negative sign.
					</p>
				</feedback>
			</choice>
			<choice>
				<statement>\frac{-6}{s^2 - 49}</statement>
				<feedback>
					<p>
						Incorrect. The correct value is <m>7</m>, not <m>6</m>.
					</p>
				</feedback>
			</choice>
		</choices>
	</exercise>
	

</reading-questions>
