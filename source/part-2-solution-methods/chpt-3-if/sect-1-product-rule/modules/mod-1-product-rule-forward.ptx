<subsection xmlns:xi="http://www.w3.org/2001/XInclude" xml:id="subsec-if-product-rule-forward"><title> Forward </title>

	<p>
		The first step of the integrating factor technique can be best described as "find the missing term that completes the product rule." With that said, let's recall the product rule. 
	</p>

	<p>
		If the derivatives of <m>f</m> and <m>g</m> exist, then
		<men xml:id="if-product-rule"> [\ f \cdot g\ ]' = f' \cdot g + f \cdot g'. </men>	
		Here are a few examples.
	</p>

	<example xml:id="example-if-product-rule-review-01">

		<p>
		  Compute
		  <m>\left[\ln x \tan x\right]'</m>.
		</p>
  
		<solution>
  
		  <p>
			Using the <xref ref="if-product-rule" text="custom"> product rule </xref>, we have
  
			<md>
				<mrow>
				\left[ \ln x \tan x \right]'
					= \ln x \cdot \left[\tan x\right]'+\left[\ln x\right]' \cdot \tan x
					= \ln x (\sec^2 x)+\left(\frac{1}{x}\right) \tan x.
			  </mrow>
			</md>
  
		  </p>
  
		</solution>
  
	</example>
  
	<example xml:id="example-if-product-rule-review-02">
  
		<p>
		  Given that <m>p</m> is some unknown function of <m>x</m>. Compute
		  <m>\displaystyle \frac{d}{dx}\left[x^3 p\right]</m>.
		</p>
  
		<solution>
  
		  <p>
			Using the <xref ref="if-product-rule" text="custom"> product rule </xref>, we have
  
			<md>
				<mrow>
				\frac{d}{dx}\left[ x^3 p \right]
					= x^3 \cdot \frac{d}{dx}\left[ p \right] + \frac{d}{dx}\left[ x^3 \right]\cdot p
					= x^3 \frac{dp}{dx} + 3x^2 p
			  </mrow>
			</md>
  
		  </p>
  
		</solution>
  
	</example>
  
	<example xml:id="example-if-product-rule-review-03">
		<p>
		  Given that <m>y</m> is some unknown function of <m>x</m>, compute <m>\left[y\sin x\right]'</m>.
		</p>
  
		<solution>
  
		  <p>
			
			<md>
				<mrow>
				\left[ y\sin x \right]'
					= y \cdot \left[ \sin x \right]'+\left[ y \right]'\cdot \sin x
					= y\cos x + y'\cdot \sin x
			  </mrow>
			</md>
  
		  </p>
		</solution>
  
	</example>
  
	
	
</subsection>